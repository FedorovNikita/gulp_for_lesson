"use strict";

[1, 2, 3, 4, 5].reduce(function (accumulator, currentValue) {
  return accumulator + currentValue;
});
console.log(2);
$('.slider').slick({
  infinite: true,
  slidesToShow: 3,
  slidesToScroll: 3
});